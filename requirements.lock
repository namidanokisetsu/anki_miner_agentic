# requirements.lock — version-pinned constraints for release bundle builds.
#
# PURPOSE: pin the exact dependency set used to build release artifacts so
# `pip install .` resolves to known-good versions rather than floating ranges.
# This is a CONSTRAINTS file (pip install -c requirements.lock), not a
# requirements file; it only bounds what pip would resolve, not what it installs.
#
# FORM: version-pin only (no --require-hashes).
# WHY: hash-pinning via `uv pip compile --universal --generate-hashes` requires
# uv (not installed) and network access to pull all OS-specific wheel hashes
# (PyQt6-Qt6 alone ships separate wheels per OS/arch). Without both, a partial
# hash lock would cause `--require-hashes` to fail on Windows/macOS runners
# because every transitive dep that appears in the lock must have its hash
# listed. A constraints-only lock is non-breaking: it pins listed packages but
# silently ignores unlisted ones, so an incomplete list never fails the build.
# See OVH-017 in docs/project-audit/2026-06-17-audit.md.
#
# To upgrade to full hash pinning, run on a machine with uv + network:
#   uv pip compile --universal --generate-hashes pyproject.toml -o requirements.lock
# then replace this file and switch release.yml to --require-hashes.
#
# Generated from: a venv with `pip install -e ".[asr]"` (Linux/Python 3.13)
# Pinned: 2026-06-24
#
# [asr] EXTRA — PINNED
# ─────────────────────────────────────
# faster-whisper and its transitive closure (ctranslate2, tokenizers,
# huggingface_hub, av, onnxruntime, numpy, etc.) are pinned below. release.yml
# installs `pip install ".[asr]" -c requirements.lock` for the Windows/macOS/
# AppImage bundles, so those versions are now reproducible.
#
# To regenerate after a dependency bump, REPLACE the pin block below — do NOT
# append (appending a fresh freeze onto the old pins creates conflicting
# duplicate versions that pip rejects as constraints):
#   python -m venv /tmp/lock-env
#   /tmp/lock-env/bin/pip install -e ".[asr]"
#   /tmp/lock-env/bin/pip freeze | grep -ivE '^-e |^anki[-_]miner==' > /tmp/pins
#   # then replace everything under "Direct runtime dependencies" with /tmp/pins
#
# Direct runtime dependencies
annotated-doc==0.0.4
anyio==4.14.0
auditok==0.1.5
av==17.1.0
budoux==0.9.0
certifi==2026.6.17
chardet==7.6.0
charset-normalizer==3.4.7
click==8.1.8
ctranslate2==4.8.0
faster-whisper==1.2.1
faust-cchardet==3.2.0
ffmpeg-python==0.2.0
ffsubsync==0.5.1
filelock==3.29.4
flatbuffers==25.12.19
fsspec==2026.6.0
fugashi==1.5.2
future==1.0.0
gTTS==2.5.4
h11==0.16.0
hf-xet==1.5.1
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.16.1
idna==3.18
lxml==6.1.1
markdown-it-py==4.2.0
mdurl==0.1.2
mpv==1.0.8
mutagen==1.48.1
numpy==2.5.0
onnxruntime==1.27.0
packaging==26.2
Pillow==12.3.0
protobuf==7.35.1
psutil==7.2.2
Pygments==2.20.0
PyQt6-Qt6==6.11.1
PyQt6==6.11.0
PyQt6_sip==13.11.1
pysubs2==1.8.1
PyYAML==6.0.3
requests==2.34.2
rich==15.0.0
setuptools==82.0.1
shellingham==1.5.4
srt==3.5.3
tokenizers==0.23.1
tqdm==4.68.3
truststore==0.10.4
typer==0.26.7
typing_extensions==4.15.0
unidic-lite==1.0.8
urllib3==2.7.0
webrtcvad-wheels==2.0.14
yt-dlp==2026.6.9
