name: Build Executables

on:
  push:
    tags:
      - "v*"
  # Manual dispatch = release DRY-RUN. Builds the full matrix + bundle smokes but
  # NEVER creates a tag, GitHub Release, or PyPI upload — every real-vs-dry
  # decision keys off github.event_name (the ci-gate and release jobs are
  # push-only). There is deliberately NO dry_run input: it would be behaviorally
  # inert (both values dispatch the same non-releasing build), and `gh workflow
  # run` passing any -f key not declared here is HTTP 422. Run via
  # scripts/release_dryrun.sh (which also proves the negatives after a green run).
  workflow_dispatch:
    inputs:
      platforms:
        description: "Which platforms to build (dispatch = dry-run; never releases)"
        type: choice
        default: linux-windows
        options:
          - linux-windows
          - all
          - linux
          - windows
          - macos

# Serialize per ref so a tag delete/re-push can't race two release runs onto the
# same uploads. Never cancel an in-flight release (publishes a GitHub Release).
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # Emit the build matrix from the committed single source of truth
  # (.github/release-matrix.json), filtered by the workflow_dispatch `platforms`
  # input. On a real tag push (and on dispatch platforms=all) ALL four entries
  # are emitted unchanged, so real releases stay byte-identical to the legacy
  # static matrix. Keeping the matrix in ONE committed file (not a second inline
  # copy) is what removes divergence risk. The per-OS rationale that used to live
  # on the static matrix now sits above the build job below.
  setup:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.filter.outputs.matrix }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Build matrix from platforms selection
        id: filter
        shell: bash
        env:
          # push (real release) always builds everything; dispatch honors the
          # input. On push, inputs.platforms is empty, so the ternary yields 'all'.
          PLATFORMS: ${{ github.event_name == 'push' && 'all' || inputs.platforms }}
        run: |
          set -euo pipefail
          case "$PLATFORMS" in
            all)           SEL='["linux","windows","macos-arm64","macos-intel"]' ;;
            linux)         SEL='["linux"]' ;;
            windows)       SEL='["windows"]' ;;
            macos)         SEL='["macos-arm64","macos-intel"]' ;;
            linux-windows) SEL='["linux","windows"]' ;;
            *) echo "::error::Unknown platforms selection: $PLATFORMS"; exit 1 ;;
          esac
          echo "Selection: $PLATFORMS -> $SEL"
          # Filter the source-of-truth entries to the selected platforms, strip the
          # internal `platform` key, and wrap as {"include":[...]} (the shape
          # fromJSON feeds strategy.matrix). Emitted entries are field-identical to
          # the legacy static include.
          MATRIX=$(jq -c --argjson sel "$SEL" \
            '{include: [ .[] | select([.platform] | inside($sel)) | del(.platform) ]}' \
            .github/release-matrix.json)
          # Fail LOUDLY at setup on an empty/malformed filter result rather than as
          # a confusing downstream fromJSON matrix-expansion error.
          echo "$MATRIX" | jq -e '.include | length > 0' > /dev/null \
            || { echo "::error::Empty matrix for selection '$PLATFORMS'"; exit 1; }
          echo "matrix=$MATRIX" >> "$GITHUB_OUTPUT"
          echo "$MATRIX" | jq .

  # A v* tag does not trigger ci.yml (push/PR-to-main only). Require a green CI
  # run on the exact tagged commit before building/publishing release artifacts.
  # Mirrored in publish.yml.
  ci-gate:
    # push-only: a workflow_dispatch is a dry-run with no tagged commit to gate on,
    # so this job is SKIPPED on dispatch and the build job reads
    # needs.ci-gate.result == 'skipped'. This job-level guard is what makes that
    # result deterministic ('skipped' on dispatch, 'success'/'failure' on a tag).
    # It INTENTIONALLY diverges from publish.yml's byte-identical "Mirrored in
    # publish.yml" ci-gate — publish.yml has no workflow_dispatch trigger, so it
    # needs no guard. Do NOT "fix the drift" by removing this: it would make a
    # dry-run wait 30 min on CI that will never run for a branch dispatch.
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      actions: read
    steps:
      - name: Require green CI on the tagged commit
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          # Wait for ci.yml on this SHA and require a "success" conclusion. The
          # tag is often pushed together with the commit, so the CI run may be
          # queued, in progress, or not yet registered when this gate starts
          # (v2.6.2 failed exactly that race). Still fail closed: no CI run
          # within the grace window, or CI finishing without success, fails.
          GRACE=300      # seconds for a CI run to appear at all
          DEADLINE=1800  # seconds total for CI to finish
          while :; do
            RUNS=$(gh api --paginate \
              "/repos/${GITHUB_REPOSITORY}/actions/workflows/ci.yml/runs?head_sha=${GITHUB_SHA}" \
              --jq '[.workflow_runs[] | {status, conclusion}]')
            SUCCESS=$(echo "$RUNS" | jq '[.[] | select(.conclusion == "success")] | length')
            PENDING=$(echo "$RUNS" | jq '[.[] | select(.status != "completed")] | length')
            TOTAL=$(echo "$RUNS" | jq 'length')
            if [ "$SUCCESS" -gt 0 ]; then
              echo "Green CI run found for tagged commit."
              break
            fi
            if [ "$TOTAL" -gt 0 ] && [ "$PENDING" -eq 0 ]; then
              echo "::error::CI completed without success for ${GITHUB_SHA}. Fix CI, then re-tag."
              exit 1
            fi
            if [ "$TOTAL" -eq 0 ] && [ "$SECONDS" -ge "$GRACE" ]; then
              echo "::error::No ci.yml run appeared for ${GITHUB_SHA} within ${GRACE}s. Tag a commit pushed to main."
              exit 1
            fi
            if [ "$SECONDS" -ge "$DEADLINE" ]; then
              echo "::error::Timed out waiting for CI on ${GITHUB_SHA}."
              exit 1
            fi
            echo "CI not complete yet (runs: ${TOTAL}, pending: ${PENDING}); retrying in 30s..."
            sleep 30
          done

  # The matrix VALUES live in .github/release-matrix.json (emitted+filtered by the
  # setup job). Per-OS rationale for those values:
  #   install_target / skip_asr_smoke drive the per-OS ASR handling. Every
  #   platform bundles local Whisper ASR via the [asr] extra EXCEPT Intel macOS:
  #   faster-whisper hard-requires onnxruntime, and onnxruntime dropped all macOS
  #   x86_64 wheels at 1.20 (the last Intel-mac builds need numpy<2, but the lock
  #   pins numpy 2.x), so [asr] is unresolvable there. The Intel build installs the
  #   base package only (skip_asr_smoke then skips the asr smoke, which would fail
  #   with no faster-whisper); the app degrades gracefully (ASR probes via
  #   find_spec → unavailable). This onnxruntime Intel-mac gap is a DESIGN
  #   exclusion the dry-run gate cannot re-catch.
  #   build_vulkan drives the from-source Vulkan pywhispercpp wheel build and its
  #   loadability smoke. "1" only on Linux + Windows: macOS stays on the CT2/Metal
  #   path (no official Vulkan artifact for macOS, and no value in MoltenVK here),
  #   so the Vulkan steps are skipped and the whispercpp smoke is skipped via
  #   skip_whispercpp_smoke: "1" on both macOS variants.
  #   Intel x86_64 macOS uses macos-15-intel: macos-13 was retired by GitHub
  #   (2025-12-04) and a retired label never gets a runner (queues forever);
  #   macos-15-intel is GitHub's recommended standard Intel replacement (free on
  #   public repos, served through ~Aug 2027 — the last x86_64 Actions image).
  #   It also drops libsvtav1 (evermeet.cx x86_64 ffmpeg ships no SVT-AV1 encoder),
  #   so animated AVIF screenshots degrade to WebP/static (probed + graceful).
  build:
    needs: [setup, ci-gate]
    # !cancelled() (not always()) so an explicit user-cancel still halts the
    # 10x-billed mac legs. The explicit needs.setup.result == 'success' guard is
    # load-bearing: !cancelled() also overrides GHA's implicit skip-on-failed-need
    # for EVERY need, so without it a setup-job failure (e.g. a jq typo) would feed
    # fromJSON('') and detonate confusingly ON A REAL TAG. ci-gate is 'skipped' on
    # dispatch and must be 'success' on a tag — the three result strings 'success'
    # / 'failure' / 'skipped' are exactly what this ternary depends on.
    if: ${{ !cancelled() && needs.setup.result == 'success' && (needs.ci-gate.result == 'success' || needs.ci-gate.result == 'skipped') }}
    strategy:
      # fail-fast is a literal sibling of matrix under strategy; it cannot be
      # injected through the fromJSON payload.
      fail-fast: false
      matrix: ${{ fromJSON(needs.setup.outputs.matrix) }}

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxcb-xinerama0 libxkbcommon-x11-0 libegl1

      - name: Install dependencies
        run: |
          # matrix.install_target is ".[asr,zh,ko]" everywhere except Intel macOS,
          # where it is ".[zh,ko]" (no [asr] — onnxruntime has no x86_64-mac wheel;
          # see the matrix note). The [asr] extra lands faster-whisper + ctranslate2
          # in the PyInstaller tree for Windows, macOS-arm64, and the AppImage
          # (Linux). requirements.lock pins transitive deps to known-good
          # versions. Version-constraint form (no --require-hashes): cross-
          # platform safe because an incomplete lock never fails the build on
          # unlisted packages, and a constraint never force-installs a package
          # the target doesn't require (so "." won't pull onnxruntime on Intel).
          # NOTE: the .deb package is system-managed and does NOT bundle
          # faster-whisper. Frozen-app guidance sends users to the ASR-capable
          # AppImage or a separate pipx install; pip cannot extend this bundle.
          pip install "${{ matrix.install_target }}" -c requirements.lock

      # PyInstaller bootloader handling. The prebuilt PyPI wheel ships a bootloader
      # binary whose hash AV vendors flag (malware reuses it), a major source of the
      # Defender false positives. On Windows, PYINSTALLER_COMPILE_BOOTLOADER=1
      # forces the sdist (--no-binary) to rebuild via waf; windows-latest has the
      # MSVC toolchain preinstalled. Other OSes keep the prebuilt wheel. Pin for
      # reproducible bundles; bump deliberately.
      - name: Install PyInstaller (Windows, bootloader from source)
        if: runner.os == 'Windows'
        env:
          PYINSTALLER_COMPILE_BOOTLOADER: "1"
        run: |
          $ErrorActionPreference = 'Stop'
          pip install --verbose --no-cache-dir --no-binary=pyinstaller pyinstaller==6.20.0 2>&1 | Tee-Object -FilePath pyinstaller-install.log
          $pipExit = $LASTEXITCODE
          if ($pipExit -ne 0) {
            exit $pipExit
          }

      - name: Assert bootloader built from source
        if: runner.os == 'Windows'
        run: |
          $ErrorActionPreference = 'Stop'
          $log = Get-Content -LiteralPath pyinstaller-install.log -Raw
          if ($log -notmatch 'waf' -or $log -notmatch 'bootloader') {
            throw 'PyInstaller install log lacks bootloader compilation evidence.'
          }

          $runw = python -c "from pathlib import Path; import PyInstaller; print(Path(PyInstaller.PACKAGEPATH) / 'bootloader' / PyInstaller.PLATFORM / 'runw.exe')"
          if ($LASTEXITCODE -ne 0) {
            exit $LASTEXITCODE
          }
          # Waf evidence proves this install compiled; existence verifies installed
          # output. mtime is unreliable after pip wheel staging/copying.
          if (-not (Test-Path -LiteralPath $runw -PathType Leaf)) {
            throw "No installed Windows-64bit runw.exe found at $runw."
          }
          $hash = (Get-FileHash -LiteralPath $runw -Algorithm SHA256).Hash
          Write-Output "Bootloader path: $runw"
          Write-Output "Bootloader SHA-256: $hash"

      - name: Install PyInstaller (Linux/macOS)
        if: runner.os != 'Windows'
        run: pip install pyinstaller==6.20.0

      - name: Extract and validate version
        id: version
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
        run: |
          set -euo pipefail
          # Single source of truth: anki_miner/__init__.py (Issue #10). pyproject
          # reads from it via [tool.setuptools.dynamic]. Parse the literal
          # directly so this step doesn't depend on the package being installed.
          CODE_VERSION=$(python -c "import re; print(re.search(r'__version__\s*=\s*[\"\x27]([^\"\x27]+)[\"\x27]', open('anki_miner/__init__.py').read()).group(1))")
          echo "Code version: $CODE_VERSION"
          if [ "$EVENT_NAME" = "push" ]; then
            # Real release: the git tag MUST equal the code version (Issue #10).
            TAG_VERSION=${GITHUB_REF_NAME#v}
            echo "Tag version: $TAG_VERSION"
            if [ "$TAG_VERSION" != "$CODE_VERSION" ]; then
              echo "::error::Version mismatch: git tag v$TAG_VERSION != anki_miner/__init__.py $CODE_VERSION"
              exit 1
            fi
          else
            # Dry-run dispatch: GITHUB_REF_NAME is a branch, not a v* tag. Drive the
            # artifact filenames (Inno/AppImage/.deb + the upload globs) off the code
            # version, but assert it is sane semver so a malformed __version__ fails
            # the dry-run loudly instead of producing garbage-named artifacts.
            if ! echo "$CODE_VERSION" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$'; then
              echo "::error::anki_miner/__init__.py __version__ is not semver: '$CODE_VERSION'"
              exit 1
            fi
          fi
          # Always emit the code version: on push it equals the tag (asserted
          # above); on dispatch there is no tag to derive from.
          echo "version=$CODE_VERSION" >> "$GITHUB_OUTPUT"

      # ---------------------------------------------------------------------
      # Fetch a full static GPL ffmpeg build per-OS and flatten ffmpeg/ffprobe
      # into vendor/ffmpeg/ at the repo root BEFORE PyInstaller runs. The spec
      # (anki_miner.spec) globs vendor/ffmpeg/ and bundles whatever it finds at
      # sys._MEIPASS/bin/ — the runtime resolver's lookup path. URLs are pinned
      # to immutable assets and every download is SHA256-verified for
      # reproducibility/supply-chain safety. These are GPL builds (NOT minimal):
      # they ship libmp3lame, libopus, libsvtav1, and libwebp/libwebp_anim,
      # which the bundled smoke step below asserts.
      # ---------------------------------------------------------------------
      - name: Fetch static ffmpeg (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          # BtbN/FFmpeg-Builds. Pin a MONTHLY snapshot tag, never a daily one:
          # BtbN prunes daily autobuilds after ~10 days (404), but keeps the
          # last-of-month snapshot long-term (15+ months of history live).
          URL="https://github.com/BtbN/FFmpeg-Builds/releases/download/autobuild-2026-05-31-13-22/ffmpeg-n8.1.1-9-g58d4114d36-linux64-gpl-8.1.tar.xz"
          SHA256="0d14781b885c491f5c3b799cbe7d3a26ba8a7eb01935483185e31ea7d79c8cd3"
          curl -fL "$URL" -o ffmpeg-dl.tar.xz
          echo "${SHA256}  ffmpeg-dl.tar.xz" | sha256sum -c -
          mkdir -p vendor/ffmpeg extracted
          tar -xf ffmpeg-dl.tar.xz -C extracted
          cp "$(find extracted -type f -path '*/bin/ffmpeg' | head -1)" vendor/ffmpeg/ffmpeg
          cp "$(find extracted -type f -path '*/bin/ffprobe' | head -1)" vendor/ffmpeg/ffprobe
          chmod +x vendor/ffmpeg/ffmpeg vendor/ffmpeg/ffprobe
          rm -rf extracted ffmpeg-dl.tar.xz
          echo "Bundled ffmpeg landed in vendor/ffmpeg/:"
          ls -la vendor/ffmpeg/

      - name: Fetch static ffmpeg (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euo pipefail
          # Two arch-native static ffmpeg/ffprobe sources, selected by runner.arch:
          #
          #   ARM64 (macos-latest) -> osxexperts.net arm64 builds. NOT version-pinned
          #     (ffmpeg81arm.zip is overwritten when a new ffmpeg minor ships), so the
          #     hard-coded SHA256 will hard-fail this step the day osxexperts updates
          #     the asset. That is intentional fail-closed behavior.
          #
          #   X64 (macos-15-intel) -> evermeet.cx Intel builds. These URLs ARE version-pinned
          #     (ffmpeg-8.1.2.zip is immutable; new versions get new filenames), so the
          #     hash only changes when we deliberately bump the version.
          #
          # In both cases ffmpeg/ffprobe are SEPARATE downloads, each a zip with a
          # single top-level binary. A checksum mismatch must block the release until a
          # maintainer re-pins the hash. Do not relax to soft-verify.
          if [ "${RUNNER_ARCH}" = "ARM64" ]; then
            FFMPEG_URL="https://www.osxexperts.net/ffmpeg81arm.zip"
            FFMPEG_SHA256="ebb82529562b71170807bbc6b0e7eb4f0b13af8cbb0e085bb9e8f6fe709598ad"
            FFPROBE_URL="https://www.osxexperts.net/ffprobe81arm.zip"
            FFPROBE_SHA256="a6640a77d38a6f0527c5b597e599cb36a3427a6931444ed80bc62542421950a1"
          else
            FFMPEG_URL="https://evermeet.cx/ffmpeg/ffmpeg-8.1.2.zip"
            FFMPEG_SHA256="e91df72a1ee7c26606f90dd2dd4dcccc6a75140ff9ea6fdd50faae828b82ba69"
            FFPROBE_URL="https://evermeet.cx/ffmpeg/ffprobe-8.1.2.zip"
            FFPROBE_SHA256="399b93f0b9862f69767afa343e90c2f48d7e7958cadbb6deb76a012d0e3b7ce3"
          fi
          mkdir -p vendor/ffmpeg
          # --retry-all-errors: osxexperts.net intermittently resets transfers
          # mid-stream (curl exit 18, partial file), which plain --retry does not
          # consider transient. Integrity still rests on the shasum check below.
          curl -fL --retry 5 --retry-delay 2 --retry-all-errors "$FFMPEG_URL" -o ffmpeg.zip
          curl -fL --retry 5 --retry-delay 2 --retry-all-errors "$FFPROBE_URL" -o ffprobe.zip
          echo "${FFMPEG_SHA256}  ffmpeg.zip" | shasum -a 256 -c -
          echo "${FFPROBE_SHA256}  ffprobe.zip" | shasum -a 256 -c -
          unzip -oj ffmpeg.zip ffmpeg -d vendor/ffmpeg
          unzip -oj ffprobe.zip ffprobe -d vendor/ffmpeg
          chmod +x vendor/ffmpeg/ffmpeg vendor/ffmpeg/ffprobe
          rm -f ffmpeg.zip ffprobe.zip
          echo "Bundled ffmpeg landed in vendor/ffmpeg/:"
          ls -la vendor/ffmpeg/

      - name: Fetch static ffmpeg (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          # BtbN/FFmpeg-Builds. Pin a MONTHLY snapshot tag, never a daily one:
          # BtbN prunes daily autobuilds after ~10 days (404), but keeps the
          # last-of-month snapshot long-term (15+ months of history live).
          $url = "https://github.com/BtbN/FFmpeg-Builds/releases/download/autobuild-2026-05-31-13-22/ffmpeg-n8.1.1-9-g58d4114d36-win64-gpl-8.1.zip"
          $sha256 = "7fe1bb1e76edf97b8ec9e84fb32b96d125d846135b6b9218af772f5af2bd9065"
          curl.exe -fL $url -o ffmpeg-dl.zip
          $actual = (Get-FileHash ffmpeg-dl.zip -Algorithm SHA256).Hash.ToLower()
          if ($actual -ne $sha256) {
            Write-Error "SHA256 mismatch for ffmpeg-dl.zip: expected $sha256 got $actual"
            exit 1
          }
          New-Item -ItemType Directory -Force -Path vendor\ffmpeg | Out-Null
          Expand-Archive -Path ffmpeg-dl.zip -DestinationPath extracted -Force
          $ff = Get-ChildItem -Path extracted -Recurse -Filter ffmpeg.exe | Select-Object -First 1
          $fp = Get-ChildItem -Path extracted -Recurse -Filter ffprobe.exe | Select-Object -First 1
          Copy-Item $ff.FullName vendor\ffmpeg\ffmpeg.exe -Force
          Copy-Item $fp.FullName vendor\ffmpeg\ffprobe.exe -Force
          Remove-Item -Recurse -Force extracted, ffmpeg-dl.zip
          Write-Host "Bundled ffmpeg landed in vendor/ffmpeg/:"
          Get-ChildItem vendor\ffmpeg

      # ---------------------------------------------------------------------
      # Fetch the alass subtitle-retiming binary per-OS and place it in
      # vendor/alass/ BEFORE PyInstaller runs. The spec (anki_miner.spec)
      # picks up whatever lands in vendor/alass/ and bundles it at
      # sys._MEIPASS/bin/ — the path alass_resolver.py checks first when
      # running frozen. macOS has no upstream v2.0.0 binary; the resolver
      # falls back to PATH (brew install alass). URLs are pinned to
      # immutable v2.0.0 release assets and every download is SHA256-
      # verified for reproducibility/supply-chain safety. alass is GPL-3.0.
      # ---------------------------------------------------------------------
      - name: Fetch alass binary (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          # kaegi/alass v2.0.0 static linux64 binary. Pinned to an immutable
          # release asset; SHA256 verified fail-closed.
          URL="https://github.com/kaegi/alass/releases/download/v2.0.0/alass-linux64"
          SHA256="7bd0b9ae7e035d3ba940eacffb21243614df36231d47f21f0b4ce42001ab7fcd"
          curl -fL "$URL" -o alass-dl
          echo "${SHA256}  alass-dl" | sha256sum -c -
          mkdir -p vendor/alass
          cp alass-dl vendor/alass/alass
          chmod +x vendor/alass/alass
          rm -f alass-dl
          mkdir -p licenses/alass
          curl -fL "https://raw.githubusercontent.com/kaegi/alass/v2.0.0/LICENSE" \
            -o licenses/alass/LICENSE
          echo "Bundled alass landed in vendor/alass/:"
          ls -la vendor/alass/

      - name: Fetch alass binary (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          # kaegi/alass v2.0.0 windows64 zip. Pinned to an immutable release
          # asset; SHA256 verified fail-closed. The zip member is
          # alass-windows64/bin/alass-cli.exe; renamed to alass.exe to match
          # the sys._MEIPASS/bin/alass.exe path alass_resolver.py expects.
          $url = "https://github.com/kaegi/alass/releases/download/v2.0.0/alass-windows64.zip"
          $sha256 = "e81a72f97f592910e909a2352d6b8c0de0801c51ac1383bad4ebf3f2ecdd2fd8"
          curl.exe -fL $url -o alass-dl.zip
          $actual = (Get-FileHash alass-dl.zip -Algorithm SHA256).Hash.ToLower()
          if ($actual -ne $sha256) {
            Write-Error "SHA256 mismatch for alass-dl.zip: expected $sha256 got $actual"
            exit 1
          }
          New-Item -ItemType Directory -Force -Path vendor\alass | Out-Null
          Expand-Archive -Path alass-dl.zip -DestinationPath alass-extracted -Force
          $exe = Get-ChildItem -Path alass-extracted -Recurse -Filter "alass-cli.exe" | Select-Object -First 1
          Copy-Item $exe.FullName vendor\alass\alass.exe -Force
          New-Item -ItemType Directory -Force -Path licenses\alass | Out-Null
          curl.exe -fL "https://raw.githubusercontent.com/kaegi/alass/v2.0.0/LICENSE" `
            -o licenses\alass\LICENSE
          Remove-Item -Recurse -Force alass-extracted, alass-dl.zip
          Write-Host "Bundled alass landed in vendor/alass/:"
          Get-ChildItem vendor\alass

      # =====================================================================
      # yt-dlp — the standalone BINARY, vendored into vendor/yt-dlp/ so the spec
      # bundles it at sys._MEIPASS/bin/. Without this a fresh packaged install
      # cannot mine YouTube at all: the resolver falls through to the bare literal
      # "yt-dlp" and CreateProcess fails.
      #
      # Version + digests come from .github/ytdlp-pin.json, shared with
      # scripts/release_preflight.sh so the two cannot drift. Digests are pinned
      # rather than read from the release's own SHA2-256SUMS (same-origin — the
      # weaker property the alass steps avoid). yt-dlp is Unlicense; its bundled
      # executables also carry ISC/MIT and others, so THIRD_PARTY_LICENSES.txt
      # ships alongside.
      #
      # The freshness gate runs first: yt-dlp ships roughly monthly and dependabot
      # cannot see a curl'd release URL, so a forgotten bump would otherwise ship a
      # stale extractor silently.
      # =====================================================================
      - name: Check the yt-dlp pin is fresh
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/check_ytdlp_pin.py

      - name: Fetch yt-dlp binary (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          PIN=.github/ytdlp-pin.json
          VERSION="$(jq -r '.version' "$PIN")"
          ASSET="$(jq -r '.assets.linux.asset' "$PIN")"
          SHA256="$(jq -r '.assets.linux.sha256' "$PIN")"
          INSTALL_AS="$(jq -r '.assets.linux.install_as' "$PIN")"
          curl -fL "https://github.com/yt-dlp/yt-dlp/releases/download/${VERSION}/${ASSET}" -o ytdlp-dl
          echo "${SHA256}  ytdlp-dl" | sha256sum -c -
          mkdir -p vendor/yt-dlp
          cp ytdlp-dl "vendor/yt-dlp/${INSTALL_AS}"
          chmod +x "vendor/yt-dlp/${INSTALL_AS}"
          rm -f ytdlp-dl
          mkdir -p licenses/yt-dlp
          curl -fL "https://raw.githubusercontent.com/yt-dlp/yt-dlp/${VERSION}/LICENSE" \
            -o licenses/yt-dlp/LICENSE
          curl -fL "https://raw.githubusercontent.com/yt-dlp/yt-dlp/${VERSION}/THIRD_PARTY_LICENSES.txt" \
            -o licenses/yt-dlp/THIRD_PARTY_LICENSES.txt
          echo "Bundled yt-dlp ${VERSION} landed in vendor/yt-dlp/:"
          ls -la vendor/yt-dlp/

      - name: Fetch yt-dlp binary (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euo pipefail
          PIN=.github/ytdlp-pin.json
          VERSION="$(jq -r '.version' "$PIN")"
          ASSET="$(jq -r '.assets.macos.asset' "$PIN")"
          SHA256="$(jq -r '.assets.macos.sha256' "$PIN")"
          INSTALL_AS="$(jq -r '.assets.macos.install_as' "$PIN")"
          # yt-dlp_macos is universal2, so this one asset serves both the arm64 and
          # Intel legs.
          curl -fL "https://github.com/yt-dlp/yt-dlp/releases/download/${VERSION}/${ASSET}" -o ytdlp-dl
          echo "${SHA256}  ytdlp-dl" | shasum -a 256 -c -
          mkdir -p vendor/yt-dlp
          cp ytdlp-dl "vendor/yt-dlp/${INSTALL_AS}"
          chmod +x "vendor/yt-dlp/${INSTALL_AS}"
          rm -f ytdlp-dl
          mkdir -p licenses/yt-dlp
          curl -fL "https://raw.githubusercontent.com/yt-dlp/yt-dlp/${VERSION}/LICENSE" \
            -o licenses/yt-dlp/LICENSE
          curl -fL "https://raw.githubusercontent.com/yt-dlp/yt-dlp/${VERSION}/THIRD_PARTY_LICENSES.txt" \
            -o licenses/yt-dlp/THIRD_PARTY_LICENSES.txt
          echo "Bundled yt-dlp ${VERSION} landed in vendor/yt-dlp/:"
          ls -la vendor/yt-dlp/

      - name: Fetch yt-dlp binary (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $pin = Get-Content .github/ytdlp-pin.json -Raw | ConvertFrom-Json
          $version = $pin.version
          $asset = $pin.assets.windows.asset
          $sha256 = $pin.assets.windows.sha256
          $installAs = $pin.assets.windows.install_as
          $url = "https://github.com/yt-dlp/yt-dlp/releases/download/$version/$asset"
          curl.exe -fL $url -o ytdlp-dl.exe
          $actual = (Get-FileHash ytdlp-dl.exe -Algorithm SHA256).Hash.ToLower()
          if ($actual -ne $sha256) {
            Write-Error "SHA256 mismatch for ${asset}: expected $sha256 got $actual"
            exit 1
          }
          New-Item -ItemType Directory -Force -Path vendor\yt-dlp | Out-Null
          Copy-Item ytdlp-dl.exe "vendor\yt-dlp\$installAs" -Force
          Remove-Item ytdlp-dl.exe -Force
          New-Item -ItemType Directory -Force -Path licenses\yt-dlp | Out-Null
          curl.exe -fL "https://raw.githubusercontent.com/yt-dlp/yt-dlp/$version/LICENSE" `
            -o licenses\yt-dlp\LICENSE
          curl.exe -fL "https://raw.githubusercontent.com/yt-dlp/yt-dlp/$version/THIRD_PARTY_LICENSES.txt" `
            -o licenses\yt-dlp\THIRD_PARTY_LICENSES.txt
          Write-Host "Bundled yt-dlp $version landed in vendor/yt-dlp/:"
          Get-ChildItem vendor\yt-dlp

      # =====================================================================
      # libmpv — fetched from the repo-owned vendor-libmpv-* release (produced
      # by .github/workflows/vendor-libmpv.yml, which is the source of truth for
      # how these are produced). Pinned URL + SHA256, fail-closed, like every
      # other fetch.
      # Lands at vendor/libmpv/ (spec dest "." → _MEIPASS root) and drops the
      # per-artifact Copyright/SOURCES.txt into licenses/libmpv/ next to the
      # committed README/COPYING.
      # =====================================================================
      - name: Fetch libmpv (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          BASE="https://github.com/${{ github.repository }}/releases/download/vendor-libmpv-20260712"
          URL="$BASE/libmpv-linux-x86_64.tar.gz"
          SHA256="5d9278463edab8f2a467f45c2c66416070d4e1543024df30fed2f721def663c1"
          curl -fL "$URL" -o libmpv-dl.tar.gz
          echo "${SHA256}  libmpv-dl.tar.gz" | sha256sum -c -
          mkdir -p vendor/libmpv licenses/libmpv extracted-libmpv
          tar -xzf libmpv-dl.tar.gz -C extracted-libmpv
          cp extracted-libmpv/libmpv.so.2 vendor/libmpv/
          cp extracted-libmpv/Copyright extracted-libmpv/SOURCES.txt licenses/libmpv/
          rm -rf extracted-libmpv libmpv-dl.tar.gz
          echo "Bundled libmpv landed in vendor/libmpv/:"
          ls -la vendor/libmpv/

      # libmpv's NEEDED closure references desktop runtime libs the headless
      # runner lacks. Installing them serves two builds at once: PyInstaller's
      # bindepend can collect the collectable ones (libva/libvdpau/liblcms2/
      # libjpeg) into the bundle at build time, and the bundled smoke's dlopen
      # can resolve the deliberately-host-only ones (libasound/libpulse — the
      # spec filters those out of the bundle; every real desktop has them).
      - name: Install libmpv runtime libraries (Linux)
        if: runner.os == 'Linux'
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libasound2 libpulse0 libva2 libva-drm2 libva-x11-2 libvdpau1 \
            libxv1 libegl1 liblcms2-2 libjpeg-turbo8

      - name: Fetch libmpv (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euo pipefail
          BASE="https://github.com/${{ github.repository }}/releases/download/vendor-libmpv-20260712"
          if [ "${RUNNER_ARCH}" = "ARM64" ]; then
            URL="$BASE/libmpv-macos-arm64.tar.gz"
            SHA256="f018f36a916890c796cb8d8779252f69851b15766326ef33a3815b9274fccff4"
          else
            URL="$BASE/libmpv-macos-x86_64.tar.gz"
            SHA256="4ea69d0dd7afdc9a55e2f3e6dbd0e4a158ae80cb0c6174770840e91ba2613dbb"
          fi
          curl -fL "$URL" -o libmpv-dl.tar.gz
          echo "${SHA256}  libmpv-dl.tar.gz" | shasum -a 256 -c -
          mkdir -p vendor/libmpv licenses/libmpv extracted-libmpv
          tar -xzf libmpv-dl.tar.gz -C extracted-libmpv
          # The whole @loader_path dylib closure rides along, flat.
          cp extracted-libmpv/*.dylib vendor/libmpv/
          cp extracted-libmpv/Copyright extracted-libmpv/SOURCES.txt licenses/libmpv/
          rm -rf extracted-libmpv libmpv-dl.tar.gz
          echo "Bundled libmpv closure landed in vendor/libmpv/ ($(ls vendor/libmpv | wc -l) files):"
          ls -la vendor/libmpv/ | head -20

      - name: Fetch libmpv (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $url = "https://github.com/${{ github.repository }}/releases/download/vendor-libmpv-20260712/libmpv-windows-x86_64.zip"
          $sha256 = "7b2dd2867d7b214e795d245e71b823089ee5a36d9eabbd159317db878d160199"
          curl.exe -fL $url -o libmpv-dl.zip
          $actual = (Get-FileHash libmpv-dl.zip -Algorithm SHA256).Hash.ToLower()
          if ($actual -ne $sha256) {
            Write-Error "SHA256 mismatch for libmpv-dl.zip: expected $sha256 got $actual"
            exit 1
          }
          New-Item -ItemType Directory -Force -Path vendor\libmpv | Out-Null
          New-Item -ItemType Directory -Force -Path licenses\libmpv | Out-Null
          Expand-Archive -Path libmpv-dl.zip -DestinationPath libmpv-extracted -Force
          Copy-Item libmpv-extracted\libmpv-2.dll vendor\libmpv\ -Force
          Copy-Item libmpv-extracted\Copyright licenses\libmpv\ -Force
          Copy-Item libmpv-extracted\SOURCES.txt licenses\libmpv\ -Force
          Remove-Item -Recurse -Force libmpv-extracted, libmpv-dl.zip
          Write-Host "Bundled libmpv landed in vendor/libmpv/:"
          Get-ChildItem vendor\libmpv

      # =====================================================================
      # Vulkan-enabled pywhispercpp — BUILD FROM SOURCE, per-OS (Linux + Windows
      # only; macOS stays CT2/Metal). No prebuilt Vulkan artifact exists on any
      # OS, so we self-build. ADDITIVE: a failure here is loud (fail-closed, no
      # `continue-on-error`) and does NOT touch the ffmpeg/alass/CT2 artifacts —
      # it only adds the Vulkan ggml backend on top of the [asr] install.
      #
      # All pins live in constraints/asr-vulkan-build.txt (pywhispercpp ref,
      # whisper.cpp submodule SHA carried by that ref, Vulkan SDK version, and the
      # auditwheel/delvewheel repair tools). Shaders compile to SPIR-V at BUILD
      # time via glslc — NO GPU is needed on the runner.
      # =====================================================================

      # Vulkan SDK / glslc supplies the headers + shader compiler at build time.
      - name: Install Vulkan SDK (Linux)
        if: runner.os == 'Linux' && matrix.build_vulkan == '1'
        # LunarG Vulkan SDK via jakoch/install-vulkan-sdk-action — SAME action and
        # pins as the Windows step below. The SDK bundles the Vulkan headers, glslc
        # (shaderc), SPIR-V tools and libvulkan.so, and the action sets VULKAN_SDK
        # + puts glslc on PATH so CMake's find_package(Vulkan) succeeds; the loader
        # is EXCLUDED from the wheel below so the user's GPU-driver loader wins at
        # runtime. No GPU/driver needed to build. NOTE: apt cannot supply glslc on
        # the pinned ubuntu-22.04 runner — shaderc is only packaged from 24.04, so
        # `apt-get install glslc` fails with "Unable to locate package glslc".
        uses: jakoch/install-vulkan-sdk-action@37effcfa045411f8bfbbda26df2fd1b3bf3436fa # v1.6.0
        with:
          vulkan_version: "1.4.350.0"
          install_runtime: false
          cache: true

      - name: Install Vulkan SDK (Windows)
        if: runner.os == 'Windows' && matrix.build_vulkan == '1'
        # LunarG Vulkan SDK via jakoch/install-vulkan-sdk-action. Pinned to an
        # immutable commit SHA (matches the SHA-pinned third-party actions in this
        # workflow) AND a Vulkan SDK version (constraints/asr-vulkan-build.txt).
        # The action sets VULKAN_SDK and puts glslc on PATH for the build step.
        uses: jakoch/install-vulkan-sdk-action@37effcfa045411f8bfbbda26df2fd1b3bf3436fa # v1.6.0
        with:
          vulkan_version: "1.4.350.0"
          install_runtime: false
          cache: true

      - name: Bundle the Vulkan loader next to libmpv (Windows)
        if: runner.os == 'Windows' && matrix.build_vulkan == '1'
        shell: pwsh
        # The vendored libmpv-2.dll has a LOAD-TIME (non-delay) import on
        # vulkan-1.dll — objdump shows vkGetInstanceProcAddr/vkCreateWin32SurfaceKHR
        # et al. in the regular Import Table, not a delay table. On a machine
        # without a Vulkan-capable GPU driver (fresh install / VM / generic display
        # driver) vulkan-1.dll is absent from System32, so LoadLibrary(libmpv-2.dll)
        # fails with WinError 126 and the video preview silently degrades to the
        # "requires mpv" notice — even though we never USE Vulkan (the player forces
        # vo=libmpv + OpenGL, hwdec=no). windows-latest HAS vulkan-1.dll in System32
        # (proven by the passing mpv smoke), which is exactly why CI never caught
        # this. Ship the loader (Apache-2.0, redistributable) next to libmpv-2.dll
        # at the _MEIPASS root — a loader with zero ICDs is harmless because the
        # Vulkan VO is never selected. Prefer the pinned LunarG SDK loader; fall
        # back to the runner's System32 copy; fail closed if neither exists.
        run: |
          $ErrorActionPreference = "Stop"
          $dll = Get-ChildItem -Path $env:VULKAN_SDK -Recurse -Filter vulkan-1.dll -ErrorAction SilentlyContinue |
                 Where-Object { $_.FullName -notmatch '(?i)\\Bin32\\' } | Select-Object -First 1
          if (-not $dll) {
            $sys = Join-Path $env:WINDIR 'System32\vulkan-1.dll'
            if (Test-Path $sys) { $dll = Get-Item $sys }
          }
          if (-not $dll) {
            Write-Error "vulkan-1.dll not found (VULKAN_SDK=$env:VULKAN_SDK, System32); libmpv-2.dll load-time dep would be missing"
            exit 1
          }
          # Committed from Vulkan-Loader's SDK-matched vulkan-sdk-1.4.350.0 tag.
          # The spec bundles this directory; fail here if binary/license pairing
          # ever drifts before PyInstaller runs.
          $license = 'licenses\vulkan-loader\LICENSE.txt'
          if (-not (Test-Path -LiteralPath $license -PathType Leaf)) {
            Write-Error "Vulkan Loader license missing at $license"
            exit 1
          }
          Copy-Item $dll.FullName vendor\libmpv\ -Force
          Write-Host "Bundled Vulkan loader from $($dll.FullName) with $license; vendor/libmpv/ now:"
          Get-ChildItem vendor\libmpv

      # Build the wheel from the pinned pywhispercpp ref with the Vulkan backend,
      # then repair it EXCLUDING the Vulkan loader. We pass NO_REPAIR=1 so
      # pywhispercpp's own repairwheel step does NOT run — its repairwheel call
      # vendors every external dep with no way to exclude the loader. We then run
      # the real repair tool ourselves, which DOES support --exclude.
      - name: Build Vulkan pywhispercpp wheel (Linux)
        if: runner.os == 'Linux' && matrix.build_vulkan == '1'
        env:
          # setup.py forwards every env var as a CMake -D, so these become
          # -DGGML_VULKAN=1 -DGGML_BACKEND_DL=1 on the whisper.cpp/ggml configure.
          GGML_VULKAN: "1"
          GGML_BACKEND_DL: "1"
          # GGML_BACKEND_DL forbids GGML_NATIVE — ggml-cpu hard-errors "GGML_NATIVE
          # is not compatible with GGML_BACKEND_DL". OFF also keeps the CPU-fallback
          # backend portable (no -march=native, so no illegal-instruction crash on
          # user CPUs lacking the runner's AVX-512), matching the portable prebuilt
          # [asr] pywhispercpp wheel this build replaces.
          GGML_NATIVE: "OFF"
          NO_REPAIR: "1"
        run: |
          set -euo pipefail
          # auditwheel repairs + excludes the loader; wheel unpacks/repacks to
          # inject the dlopen backend (see constraints/asr-vulkan-build.txt).
          # patchelf comes from PyPI, not apt: auditwheel 6.8.1 requires
          # >= 0.14.5 and ubuntu-22.04 ships 0.14.3 (see the constraints file).
          pip install -c constraints/asr-vulkan-build.txt auditwheel wheel patchelf
          patchelf --version
          mkdir -p dist-vulkan-raw dist-vulkan
          # git+ (not the PyPI sdist) so the whisper.cpp/pybind11 submodules are
          # fetched at the SHAs the pinned tag records. --no-build-isolation would
          # drop the build deps pywhispercpp declares, so keep isolation ON.
          # --no-clean KEEPS the build tree: ggml builds the ggml-vulkan MODULE
          # backend into build/temp.*/_pywhispercpp/bin (NOT the wheel), and the
          # SHARED libs (libwhisper.so.1, libggml*) land in build/lib.* which is
          # exactly the extension's DT_RUNPATH — both are needed below.
          pip wheel --no-clean --no-deps -w dist-vulkan-raw \
            "pywhispercpp @ git+https://github.com/absadiki/pywhispercpp.git@v1.5.0"
          RAW=$(find dist-vulkan-raw -name 'pywhispercpp-*.whl' | head -1)
          echo "Raw (unrepaired) wheel: $RAW"
          # Derive the exact build lib dir from the raw extension's RUNPATH
          # (self-anchoring — avoids globbing /tmp for the right build tree).
          mkdir -p _extprobe
          EXT=$(unzip -Z1 "$RAW" | grep -E '^_pywhispercpp.*\.so$' | head -1)
          unzip -o "$RAW" "$EXT" -d _extprobe >/dev/null
          BUILD_LIBDIR=$(readelf -d "_extprobe/$EXT" \
            | awk '/RUNPATH|RPATH/{gsub(/[][]/,"",$NF); print $NF}' | head -1)
          echo "Build lib dir (RUNPATH): $BUILD_LIBDIR"
          test -e "$BUILD_LIBDIR/libwhisper.so.1" \
            || { echo "::error::libwhisper.so.1 not found under $BUILD_LIBDIR"; exit 1; }
          # The MODULE backend dir is the sibling temp.* of the lib.* dir.
          BUILD_ROOT=$(dirname "$BUILD_LIBDIR")
          TAG=$(basename "$BUILD_LIBDIR" | sed 's/^lib\.//')
          BACKEND_BIN="$BUILD_ROOT/temp.$TAG/_pywhispercpp/bin"
          # Both GGML_BACKEND_DL modules build here: the Vulkan backend AND the
          # CPU-fallback backend (libggml-cpu — a separate dlopen module, NOT
          # compiled into libggml-base). Inject both; fail closed if either is
          # missing so a CPU-less or Vulkan-less wheel never ships silently.
          mapfile -t BACKEND_LIBS < <(find "$BACKEND_BIN" \( -name 'libggml-vulkan*.so*' -o -name 'libggml-cpu*.so*' \) 2>/dev/null)
          find "$BACKEND_BIN" -name 'libggml-vulkan*.so*' -print -quit | grep -q . \
            || { echo "::error::libggml-vulkan*.so not built under $BACKEND_BIN"; exit 1; }
          find "$BACKEND_BIN" -name 'libggml-cpu*.so*' -print -quit | grep -q . \
            || { echo "::error::libggml-cpu*.so not built under $BACKEND_BIN (GGML_BACKEND_DL CPU module missing)"; exit 1; }
          # Inject the backend MODULE lib(s) into the raw wheel + regen RECORD.
          rm -rf _unpacked && wheel unpack -d _unpacked "$RAW"
          WHDIR=$(ls -d _unpacked/pywhispercpp-*/)
          cp "${BACKEND_LIBS[@]}" "$WHDIR"
          rm -f "$WHDIR"pywhispercpp-*.dist-info/RECORD
          mkdir -p dist-vulkan-inj && wheel pack -d dist-vulkan-inj "$WHDIR"
          INJ=$(find dist-vulkan-inj -name 'pywhispercpp-*.whl' | head -1)
          # Repair: LD_LIBRARY_PATH=build lib dir lets auditwheel locate
          # libwhisper.so.1 (+libggml*); EXCLUDE the Vulkan loader (bind the user's
          # driver loader at runtime) AND the injected backend modules (ggml-vulkan
          # + ggml-cpu — each repaired in place, NEEDED repointed into .libs, kept
          # top-level, but not grafted as a dependency: both are dlopen modules).
          LD_LIBRARY_PATH="$BUILD_LIBDIR${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}" \
            auditwheel repair \
              --exclude libvulkan.so.1 --exclude libvulkan.so \
              --exclude libggml-vulkan.so \
              --exclude libggml-cpu.so \
              -w dist-vulkan "$INJ"
          WHEEL=$(find dist-vulkan -name 'pywhispercpp-*.whl' | head -1)
          echo "VULKAN_WHEEL=$WHEEL" >> "$GITHUB_ENV"
          echo "Repaired Vulkan wheel: $WHEEL"
          # Loud, fail-closed sanity: the loader must NOT be inside the wheel, and
          # the ggml-vulkan backend MUST be.
          unzip -l "$WHEEL" | tee wheel_contents.txt
          if grep -Eiq 'libvulkan\.so' wheel_contents.txt; then
            echo "::error::Vulkan loader leaked into the wheel — exclude failed"
            exit 1
          fi
          if ! grep -Eiq 'libggml-vulkan' wheel_contents.txt; then
            echo "::error::ggml-vulkan backend missing from the wheel — Vulkan build did not produce the backend"
            exit 1
          fi
          if ! grep -Eiq 'libggml-cpu' wheel_contents.txt; then
            echo "::error::ggml-cpu backend missing from the wheel — CPU fallback backend not bundled"
            exit 1
          fi

      # Ninja generator on Windows: the from-source pywhispercpp build otherwise
      # picks the Visual Studio / MSBuild generator, whose FileTracker writes
      # per-target .tlog paths under pip's deep temp tree that cross Windows'
      # 260-char MAX_PATH and hard-fail with "FileTracker : error FTK1011"
      # (deterministic — the ABI-probe .tlog is 262 chars). Ninja emits no .tlog
      # at all, killing the whole FTK1011 class including the nested
      # vulkan-shaders-gen ExternalProject (which inherits the generator), and
      # pywhispercpp's setup.py drops "-A x64" when CMAKE_GENERATOR=Ninja. Ninja
      # does NOT auto-load vcvars the way the VS generator does, so cl.exe must be
      # put on PATH first — hence the MSVC dev-env step below. TMP/TEMP also point
      # at the short RUNNER_TEMP (D:\a\_temp) so no build path nears MAX_PATH.
      - name: Set up MSVC dev environment (Windows, for Ninja)
        if: runner.os == 'Windows' && matrix.build_vulkan == '1'
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1.13.0
        with:
          arch: x64

      - name: Build Vulkan pywhispercpp wheel (Windows)
        if: runner.os == 'Windows' && matrix.build_vulkan == '1'
        shell: pwsh
        env:
          GGML_VULKAN: "1"
          GGML_BACKEND_DL: "1"
          # GGML_BACKEND_DL forbids GGML_NATIVE + keeps the CPU fallback portable
          # (see the Linux step above for the full rationale).
          GGML_NATIVE: "OFF"
          NO_REPAIR: "1"
          # Force Ninja so no MSBuild FileTracker .tlog is written (see comment
          # above) — the actual fix for the FTK1011 MAX_PATH failure.
          CMAKE_GENERATOR: "Ninja"
          # cl on PATH (via msvc-dev-cmd) as the compiler — the vulkan-shaders-gen
          # ExternalProject's fresh CMake reads CC/CXX from the env (it does NOT
          # inherit -DCMAKE_C_COMPILER), so this makes its compiler detection
          # deterministic on the VS-bundled ninja.
          CC: "cl"
          CXX: "cl"
          # Short temp base for any pip/tool temp; the primary MAX_PATH fix is the
          # short C:\ws source clone below.
          TMP: ${{ runner.temp }}
          TEMP: ${{ runner.temp }}
        run: |
          $ErrorActionPreference = "Stop"
          # Build toolchain into THIS env (no pip build isolation). NO pip `ninja` —
          # a real VS ninja is on PATH via msvc-dev-cmd; pip's ninja.exe is broken in
          # the sub-build spawn context.
          pip install -c constraints/asr-vulkan-build.txt delvewheel `
            "setuptools>=42" wheel "cmake>=3.12" repairwheel "setuptools-scm>=8"
          # Build from a SHORT source dir. Under pip's default git+ build path
          # (<temp>\pip-wheel-XXXX\pywhispercpp_<32hex>\build\...) the vulkan-shaders-
          # gen ExternalProject's TryCompile working directory reaches EXACTLY 260
          # chars, so Windows CreateProcess fails to spawn ninja with ENOENT ("no
          # such file or directory") and the host-tool build aborts ("CMAKE_C_
          # COMPILER not set, after EnableLanguage"). Cloning to C:\ws and building
          # in-tree (setup.py — no pip temp copy) drops that path to ~190, well under
          # MAX_PATH. Ninja (via CMAKE_GENERATOR) still avoids the FTK1011 .tlog class.
          $repo = $PWD.Path
          if (Test-Path C:\ws) { Remove-Item -Recurse -Force C:\ws }
          git clone --branch v1.5.0 --recurse-submodules `
            https://github.com/absadiki/pywhispercpp.git C:\ws
          if ($LASTEXITCODE -ne 0) { Write-Error "pywhispercpp clone failed"; exit 1 }
          New-Item -ItemType Directory -Force -Path dist-vulkan-raw, dist-vulkan | Out-Null
          Push-Location C:\ws
          python setup.py bdist_wheel --dist-dir "$repo\dist-vulkan-raw"
          if ($LASTEXITCODE -ne 0) { Pop-Location; Write-Error "pywhispercpp wheel build failed"; exit 1 }
          Pop-Location
          $raw = Get-ChildItem dist-vulkan-raw -Filter 'pywhispercpp-*.whl' | Select-Object -First 1
          Write-Host "Raw (unrepaired) wheel: $($raw.FullName)"
          # The build put whisper.dll / ggml*.dll / ggml-vulkan.dll / ggml-cpu.dll in
          # the CMake bin dir (NOT the wheel — setup.py does not bundle them), so point
          # delvewheel there. It grafts the .pyd's DLL deps (whisper, ggml, ggml-base)
          # automatically. Under GGML_BACKEND_DL both ggml-vulkan.dll AND ggml-cpu.dll
          # are dlopen MODULES, NOT import-table deps of _pywhispercpp.pyd, so
          # delvewheel's dependency walk does NOT reach either — both are force-grafted
          # with --include. EXCLUDE the Vulkan loader so ggml-vulkan binds the user's
          # driver loader.
          $vulkanDll = Get-ChildItem C:\ws\build -Recurse -Filter ggml-vulkan.dll -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $vulkanDll) { Write-Error "ggml-vulkan.dll not found under C:\ws\build"; exit 1 }
          $cpuDll = Get-ChildItem C:\ws\build -Recurse -Filter ggml-cpu.dll -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $cpuDll) { Write-Error "ggml-cpu.dll not found under C:\ws\build (GGML_BACKEND_DL CPU module missing)"; exit 1 }
          $bin = $vulkanDll.DirectoryName
          Write-Host "Backend DLL dir: $bin"
          # --no-mangle-all: keep every vendored DLL's ORIGINAL name. delvewheel
          # normally hashes names (ggml.dll -> ggml-<hash>.dll) and patches importers'
          # import tables, but it does NOT patch the imports of --include'd DLLs. The
          # ggml-cpu/ggml-vulkan MODULES are dlopen'd (not in any import table) so their
          # own imports of ggml-base.dll / ggml.dll would be left pointing at the now-
          # mangled names -> the module fails to load at runtime -> whisper reports 0
          # backends and GGML_ASSERT(device) aborts (caught by the strengthened smoke).
          # Keeping the whole ggml chain unmangled makes every intra-ggml import resolve
          # against one consistent set of DLLs in the frozen tree.
          delvewheel repair --no-mangle-all --exclude vulkan-1.dll `
            --include ggml-vulkan.dll --include ggml-cpu.dll `
            --add-path "$bin;$env:VULKAN_SDK\Bin" `
            -w dist-vulkan $raw.FullName
          $wheel = Get-ChildItem dist-vulkan -Filter 'pywhispercpp-*.whl' | Select-Object -First 1
          "VULKAN_WHEEL=$($wheel.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Append
          Write-Host "Repaired Vulkan wheel: $($wheel.FullName)"
          # Loud, fail-closed sanity: the loader must NOT be inside the wheel, and
          # the ggml-vulkan backend MUST be.
          python -c "import zipfile,sys; n=zipfile.ZipFile(sys.argv[1]).namelist(); print('\n'.join(n))" $wheel.FullName | Tee-Object wheel_contents.txt
          if (Select-String -Path wheel_contents.txt -Pattern 'vulkan-1\.dll' -Quiet) {
            Write-Error "Vulkan loader leaked into the wheel — exclude failed"; exit 1
          }
          if (-not (Select-String -Path wheel_contents.txt -Pattern 'ggml-vulkan' -Quiet)) {
            Write-Error "ggml-vulkan backend missing from the wheel — Vulkan build did not produce the backend"; exit 1
          }
          if (-not (Select-String -Path wheel_contents.txt -Pattern 'ggml-cpu' -Quiet)) {
            Write-Error "ggml-cpu backend missing from the wheel — CPU fallback backend not bundled"; exit 1
          }

      # Install the built Vulkan wheel into the SAME env PyInstaller bundles from,
      # BEFORE PyInstaller runs, replacing any CPU pywhispercpp. --force-reinstall
      # so a pywhispercpp pulled in by [asr] (now or in future) is overwritten by
      # the Vulkan build. shell: bash on both OSes (set -euo pipefail and
      # ${VULKAN_WHEEL} expansion; windows-latest has bash).
      - name: Install Vulkan wheel into bundle env
        if: matrix.build_vulkan == '1'
        shell: bash
        run: |
          set -euo pipefail
          echo "Installing Vulkan pywhispercpp wheel: ${VULKAN_WHEEL}"
          pip install --force-reinstall --no-deps "${VULKAN_WHEEL}"
          # --no-deps above (so the faster-whisper/ctranslate2 pins from [asr] are
          # NOT disturbed) means pywhispercpp's own runtime deps are not installed.
          # pywhispercpp.model imports pywhispercpp.constants (-> platformdirs) and
          # pywhispercpp.utils (-> requests, tqdm) at module load, so without these
          # the frozen bundle would raise ModuleNotFoundError on the first
          # `import pywhispercpp.model` and the Vulkan engine would silently never
          # activate. Install them (pure-Python, tiny) so PyInstaller collects them.
          # Adding them to the [asr-vulkan] extra is NOT enough — the release job
          # installs [asr], not [asr-vulkan].
          pip install platformdirs requests tqdm

      - name: Build with PyInstaller
        run: pyinstaller anki_miner.spec

      # macOS only, and it must run AFTER the build. yt-dlp_macos is a
      # PyInstaller onefile whose payload is a PKG archive appended past the end
      # of the Mach-O image, so every Mach-O rewrite PyInstaller performs on a
      # collected binary (arch thinning, install_name_tool, ad-hoc re-signing)
      # truncates it -- the binary then starts and dies with
      #   [PYI-12119:ERROR] Could not load PyInstaller's embedded PKG archive
      # anki_miner.spec skips it on darwin for that reason; this copies it in
      # byte for byte instead. Fails closed: the bundle smoke's youtube leg
      # execs this exact path, and the Intel leg is what caught the regression.
      - name: Vendor yt-dlp into the bundle (macOS)
        if: runner.os == 'macOS'
        run: |
          set -euo pipefail
          src="vendor/yt-dlp/yt-dlp"
          dest="dist/AnkiMiner/_internal/bin"
          test -f "$src" || { echo "::error::$src missing — the vendor step did not run"; exit 1; }
          mkdir -p "$dest"
          cp "$src" "$dest/yt-dlp"
          chmod +x "$dest/yt-dlp"
          cmp "$src" "$dest/yt-dlp" || { echo "::error::vendored yt-dlp differs from its source"; exit 1; }
          "$dest/yt-dlp" --version

      # Fetch a tiny ggml acoustic model so the whispercpp smoke can do a REAL
      # construct+decode (the only assertion that catches DEFECT 1's SIGABRT and
      # DEFECT 2's missing CPU backend). Vulkan jobs only (Linux + Windows);
      # macOS has no Vulkan wheel and leaves BUNDLE_SMOKE_GGML_MODEL empty ->
      # the smoke stays import-only. ggml-tiny.bin is ~74 MB (NOT the 1 GB
      # large-v3); the 1 s silent decode is sub-second. Pinned to an immutable
      # HF blob + SHA256-verified fail-closed (matches the alass/nfpm/ffmpeg
      # pinned downloads). FAIL-OPEN with a LOUD warning if the download itself
      # fails (HF outage must not red a release) — but if the file DID download,
      # the sha256 MUST match or the step fails.
      - name: Fetch ggml-tiny model for whispercpp decode smoke (Linux)
        if: runner.os == 'Linux' && matrix.build_vulkan == '1'
        shell: bash
        run: |
          set -uo pipefail
          URL="https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.bin"
          SHA256="be07e048e1e599ad46341c8d2a135645097a538221678b7acdd1b1919c6e1b21"
          DEST="${RUNNER_TEMP}/ggml-tiny.bin"
          if ! curl -fL "$URL" -o "$DEST"; then
            echo "::warning::ggml-tiny.bin download failed (HF outage?) — whispercpp smoke will run import-only, not decode. NOT failing the release."
            rm -f "$DEST"
            exit 0
          fi
          echo "${SHA256}  ${DEST}" | sha256sum -c - \
            || { echo "::error::ggml-tiny.bin sha256 mismatch — refusing to smoke against a corrupt model"; exit 1; }
          echo "SMOKE_GGML_MODEL_PATH=${DEST}" >> "$GITHUB_ENV"
          echo "Fetched ggml-tiny.bin for decode smoke: ${DEST}"

      - name: Fetch ggml-tiny model for whispercpp decode smoke (Windows)
        if: runner.os == 'Windows' && matrix.build_vulkan == '1'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $url = "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-tiny.bin"
          $sha256 = "be07e048e1e599ad46341c8d2a135645097a538221678b7acdd1b1919c6e1b21"
          $dest = Join-Path $env:RUNNER_TEMP "ggml-tiny.bin"
          curl.exe -fL $url -o $dest
          if ($LASTEXITCODE -ne 0) {
            Write-Host "::warning::ggml-tiny.bin download failed (HF outage?) — whispercpp smoke will run import-only, not decode. NOT failing the release."
            if (Test-Path $dest) { Remove-Item -Force $dest }
            exit 0
          }
          $actual = (Get-FileHash $dest -Algorithm SHA256).Hash.ToLower()
          if ($actual -ne $sha256) {
            Write-Error "ggml-tiny.bin sha256 mismatch (expected $sha256 got $actual) — refusing to smoke against a corrupt model"; exit 1
          }
          "SMOKE_GGML_MODEL_PATH=$dest" | Out-File -FilePath $env:GITHUB_ENV -Append
          Write-Host "Fetched ggml-tiny.bin for decode smoke: $dest"

      # Fetch the Korean model so the ko language smoke has something to tokenize
      # with. Unlike zh, the ko model is NOT bundle content: it is an ~88 MB
      # in-app download pack (anki_miner/services/ko_model_installer.py), so the
      # smoke has to be handed one or the leg cannot run at all. Every platform
      # needs it — BUNDLE_SMOKE_LANGS carries ko on all legs. Pinned to the same
      # immutable PyPI sdist + SHA256 the installer pins, verified fail-closed.
      # FAIL-OPEN with a LOUD warning if the download itself fails (a PyPI outage
      # must not red a release) — bundle_smoke.sh then skips the ko leg rather
      # than failing it.
      - name: Fetch the Korean model for the ko smoke (Linux/macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -uo pipefail
          URL="https://files.pythonhosted.org/packages/77/59/28403890c5f757254bf2068ff321fb3e656fb2e5658a3de8bfc092e4fd83/kiwipiepy_model-0.23.0.tar.gz"
          SHA256="498a22f5585e6c4a162423d7557eb3ee3f71cddc6e0aeb2650c50467e85933e2"
          ARCHIVE="${RUNNER_TEMP}/kiwipiepy_model.tar.gz"
          EXTRACTED="${RUNNER_TEMP}/kiwipiepy_model_sdist"
          if ! curl -fL "$URL" -o "$ARCHIVE"; then
            echo "::warning::kiwipiepy_model download failed (PyPI outage?) — the ko bundle smoke will be skipped. NOT failing the release."
            rm -f "$ARCHIVE"
            exit 0
          fi
          # macOS ships shasum, not sha256sum; both read the same input format.
          if command -v sha256sum >/dev/null 2>&1; then
            echo "${SHA256}  ${ARCHIVE}" | sha256sum -c - \
              || { echo "::error::kiwipiepy_model sha256 mismatch — refusing to smoke against a corrupt model"; exit 1; }
          else
            echo "${SHA256}  ${ARCHIVE}" | shasum -a 256 -c - \
              || { echo "::error::kiwipiepy_model sha256 mismatch — refusing to smoke against a corrupt model"; exit 1; }
          fi
          rm -rf "$EXTRACTED"
          mkdir -p "$EXTRACTED"
          tar -xzf "$ARCHIVE" -C "$EXTRACTED"
          MODEL="${EXTRACTED}/kiwipiepy_model-0.23.0/kiwipiepy_model"
          test -f "${MODEL}/sj.morph" || { echo "::error::kiwipiepy_model sdist has no kiwipiepy_model/sj.morph"; exit 1; }
          echo "SMOKE_KO_MODEL_PATH=${MODEL}" >> "$GITHUB_ENV"
          echo "Fetched the Korean model for the ko smoke: ${MODEL}"

      - name: Fetch the Korean model for the ko smoke (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $url = "https://files.pythonhosted.org/packages/77/59/28403890c5f757254bf2068ff321fb3e656fb2e5658a3de8bfc092e4fd83/kiwipiepy_model-0.23.0.tar.gz"
          $sha256 = "498a22f5585e6c4a162423d7557eb3ee3f71cddc6e0aeb2650c50467e85933e2"
          $archive = Join-Path $env:RUNNER_TEMP "kiwipiepy_model.tar.gz"
          $extracted = Join-Path $env:RUNNER_TEMP "kiwipiepy_model_sdist"
          curl.exe -fL $url -o $archive
          if ($LASTEXITCODE -ne 0) {
            Write-Host "::warning::kiwipiepy_model download failed (PyPI outage?) — the ko bundle smoke will be skipped. NOT failing the release."
            if (Test-Path $archive) { Remove-Item -Force $archive }
            exit 0
          }
          $actual = (Get-FileHash $archive -Algorithm SHA256).Hash.ToLower()
          if ($actual -ne $sha256) {
            Write-Error "kiwipiepy_model sha256 mismatch (expected $sha256 got $actual) — refusing to smoke against a corrupt model"; exit 1
          }
          if (Test-Path $extracted) { Remove-Item -Recurse -Force $extracted }
          New-Item -ItemType Directory -Path $extracted | Out-Null
          tar.exe -xzf $archive -C $extracted
          $model = Join-Path $extracted "kiwipiepy_model-0.23.0\kiwipiepy_model"
          if (-not (Test-Path (Join-Path $model "sj.morph"))) {
            Write-Error "kiwipiepy_model sdist has no kiwipiepy_model/sj.morph"; exit 1
          }
          "SMOKE_KO_MODEL_PATH=$model" | Out-File -FilePath $env:GITHUB_ENV -Append
          Write-Host "Fetched the Korean model for the ko smoke: $model"

      # Bundle-validation smokes, all three in one shared script so the local
      # preflight (scripts/release_preflight.sh) and CI assert identically and
      # cannot drift. The script runs, fail-closed:
      #   - youtube : yt-dlp extractor registry survived collect_all (no network)
      #   - asr     : faster-whisper + ctranslate2 + av native libs resolve, no
      #               model download (HF_HUB_OFFLINE=1 set inside the script). A
      #               model download in CI is never acceptable (150 MB+, runtime-
      #               provided). This is the smoke that caught the v2.7.1 av miss.
      #               SKIPPED on Intel macOS (BUNDLE_SMOKE_SKIP_ASR=1) — that
      #               build ships no [asr] extra, so the smoke would fail.
      #   - ffmpeg  : bundled ffmpeg ships libmp3lame/libopus/libsvtav1/libwebp/
      #               libwebp_anim (anim asserted separately so a still-only webp
      #               build can't pass while animated screenshots fail at runtime).
      #   - whispercpp-vulkan : IMPORT/LOADABILITY ONLY — the from-source Vulkan
      #               pywhispercpp wheel was bundled (ggml-vulkan backend lib
      #               present), the frozen binary's hidden Vulkan probe prints an
      #               integer device count (0 expected on GPU-less runners), AND
      #               the frozen binary actually `import pywhispercpp.model`s (the
      #               real runtime chain that pulls platformdirs/requests/tqdm — so
      #               a missing transitive dep fails the gate). It proves NOTHING
      #               about GPU transcription. SKIPPED on macOS
      #               (skip_whispercpp_smoke: "1" on both macOS variants) —
      #               macOS stays on CT2/Metal and ships no Vulkan wheel.
      # shell: bash on every OS — windows-latest has bash on PATH; the script
      # finds AnkiMiner(.exe) and ffmpeg(.exe) itself.
      - name: Bundled smokes (youtube + asr + mpv + ffmpeg encoders + whispercpp-vulkan)
        shell: bash
        env:
          BUNDLE_SMOKE_SKIP_ASR: ${{ matrix.skip_asr_smoke }}
          BUNDLE_SMOKE_FFMPEG_ENCODERS: ${{ matrix.ffmpeg_encoders }}
          BUNDLE_SMOKE_SKIP_WHISPERCPP: ${{ matrix.skip_whispercpp_smoke }}
          # mpv: all four legs bundle libmpv; the flag exists as the house-style
          # escape hatch only. Smoke = presence + frozen dlopen probe (MPV_PROBE_OK).
          BUNDLE_SMOKE_SKIP_MPV: ${{ matrix.skip_mpv_smoke }}
          # ggml-tiny.bin path from the fetch step (Vulkan jobs only) — enables the
          # whispercpp construct+decode smoke (DEFECT 1/2 coverage). Empty on macOS
          # and when the HF download fell open, so the smoke stays import-only there.
          BUNDLE_SMOKE_GGML_MODEL: ${{ env.SMOKE_GGML_MODEL_PATH }}
          # Korean model path from the fetch step above. The bundle excludes the
          # model (it downloads in-app), so the smoke seeds this into its isolated
          # home before the ko leg. Empty when the PyPI download fell open, and
          # the ko leg then skips instead of failing.
          BUNDLE_SMOKE_KO_MODEL: ${{ env.SMOKE_KO_MODEL_PATH }}
          # Mining-language tokenizer legs (opt-in in bundle_smoke.sh so the
          # local invocation-count test stays valid).
          BUNDLE_SMOKE_LANGS: zh ko
        run: scripts/bundle_smoke.sh dist/AnkiMiner

      # packaging/linux-launcher.sh became the AppImage's AppRun and the .deb's
      # /usr/bin entrypoint. The bundled smoke above execs dist/AnkiMiner
      # DIRECTLY, so it proves nothing about the shim — and a bug there does not
      # degrade anything, it bricks the artifact. Runs before the AppImage build
      # so a broken shim fails the matrix instead of shipping.
      - name: Launcher shim smoke (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: scripts/launcher_smoke.sh dist/AnkiMiner

      - name: Install Inno Setup (pinned)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Chocolatey's innosetup package lags (6.x); install the official
          # 7.0.2 from the immutable jrsoftware/issrc release asset, pinned by
          # SHA-256 (hash cross-checked against the GitHub API asset digest).
          $url = 'https://github.com/jrsoftware/issrc/releases/download/is-7_0_2/innosetup-7.0.2-x64.exe'
          $expectedSha = '5ad54ca3def786f8f4212552e54cc6d8d61329e2d24a1cfee0571d42c2684ff1'
          $setup = Join-Path $env:RUNNER_TEMP 'innosetup-7.0.2-x64.exe'
          Invoke-WebRequest -Uri $url -OutFile $setup
          $actualSha = (Get-FileHash -Algorithm SHA256 $setup).Hash.ToLowerInvariant()
          if ($actualSha -ne $expectedSha) {
            throw "Inno Setup download hash mismatch: expected $expectedSha, got $actualSha"
          }
          $installDir = 'C:\InnoSetup7'
          $proc = Start-Process -FilePath $setup -ArgumentList @(
            '/VERYSILENT', '/SUPPRESSMSGBOXES', '/NORESTART', "/DIR=$installDir"
          ) -Wait -PassThru
          if ($proc.ExitCode -ne 0) {
            throw "Inno Setup installer exited with $($proc.ExitCode)"
          }
          $iscc = Join-Path $installDir 'ISCC.exe'
          if (-not (Test-Path $iscc)) {
            throw "ISCC.exe not found at $iscc after install"
          }
          # The SHA-256 pin above is the authoritative 7.0.2 identity check.
          # Do NOT assert (Get-Item $iscc).VersionInfo.ProductVersion: Inno Setup
          # 7.x ISCC.exe ships ProductVersion 0.0.0.0, and its runtime banner
          # (ISCC.dpr ShowBanner) carries only the major "Inno Setup 7", never
          # "7.0.2". Functional sanity check that ISCC runs and self-reports v7;
          # `iscc /?` prints the banner to stdout then Halt(1), so the native
          # exit code 1 is expected and not an error.
          $banner = (& $iscc /? 2>$null | Select-Object -First 1)
          if ($banner -notlike 'Inno Setup 7*') {
            throw "Unexpected ISCC banner '$banner' at $iscc"
          }
          $installDir | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: Build Windows installer
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          iscc /DAppVersion=${{ steps.version.outputs.version }} packaging\innosetup\anki_miner.iss
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          # Fast, guard-bearing CI probe. Explicit Windows artifact upload below
          # names only EXPECTED_VERSION, so this 0.0.1 installer is never shipped.
          iscc /DAppVersion=0.0.1 /DProbeBuild=1 /Odist packaging\innosetup\anki_miner.iss
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Smoke Windows installer
        if: runner.os == 'Windows'
        shell: pwsh
        timeout-minutes: 25
        env:
          INSTALLER_PATH: dist/AnkiMiner-${{ steps.version.outputs.version }}-Windows-x86_64-Setup.exe
          EXPECTED_VERSION: ${{ steps.version.outputs.version }}
        run: |
          $ErrorActionPreference = 'Stop'
          Set-StrictMode -Version Latest
          . (Join-Path $PWD 'scripts/windows_installer_smoke_lib.ps1')
          Invoke-WindowsInstallerSmokeLegs12 `
            -InstallerPath $env:INSTALLER_PATH `
            -ExpectedVersion $env:EXPECTED_VERSION `
            -RepoRoot $PWD.Path

      - name: Smoke Windows installer upgrade (leg3)
        if: runner.os == 'Windows'
        shell: pwsh
        timeout-minutes: 30
        env:
          INSTALLER_PATH: dist/AnkiMiner-${{ steps.version.outputs.version }}-Windows-x86_64-Setup.exe
          PROBE_INSTALLER_PATH: dist/AnkiMiner-0.0.1-Windows-x86_64-Setup.exe
          EXPECTED_VERSION: ${{ steps.version.outputs.version }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          $ErrorActionPreference = 'Stop'
          Set-StrictMode -Version Latest
          . (Join-Path $PWD 'scripts/windows_installer_smoke_lib.ps1')
          Invoke-WindowsInstallerSmokeLeg3 `
            -InstallerPath $env:INSTALLER_PATH `
            -ProbeInstallerPath $env:PROBE_INSTALLER_PATH `
            -ExpectedVersion $env:EXPECTED_VERSION `
            -Repository $env:GITHUB_REPOSITORY `
            -RepoRoot $PWD.Path

      - name: Upload Windows installer smoke logs
        if: ${{ failure() && runner.os == 'Windows' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ matrix.artifact_name }}-installer-smoke-logs
          if-no-files-found: warn
          path: |
            ${{ runner.temp }}/setup1.log
            ${{ runner.temp }}/uninstall1.log
            ${{ runner.temp }}/setup2.log
            ${{ runner.temp }}/uninstall2.log
            ${{ runner.temp }}/uninstall-cleanup.log
            ${{ runner.temp }}/setup-old.log
            ${{ runner.temp }}/setup-upgrade.log
            ${{ runner.temp }}/setup-downgrade-probe.log
            ${{ runner.temp }}/uninstall-upgrade.log

      - name: Build AppImage (Linux)
        if: runner.os == 'Linux'
        run: |
          chmod +x packaging/appimage/build-appimage.sh
          packaging/appimage/build-appimage.sh ${{ steps.version.outputs.version }}

      - name: Install nfpm (Linux)
        if: runner.os == 'Linux'
        run: |
          set -euo pipefail
          # Version-pinned; add SHA256 verification (matches goreleaser's
          # official checksums.txt for v2.46.0). Fail-closed on mismatch.
          NFPM_SHA256="43b4cb72cde2d6e61c02e5b330e3276882252bf67c057e089957f9dbd2c8de42"
          wget -q https://github.com/goreleaser/nfpm/releases/download/v2.46.0/nfpm_2.46.0_Linux_x86_64.tar.gz
          echo "${NFPM_SHA256}  nfpm_2.46.0_Linux_x86_64.tar.gz" | sha256sum -c -
          tar -xzf nfpm_2.46.0_Linux_x86_64.tar.gz nfpm
          chmod +x nfpm

      - name: Build .deb package (Linux)
        if: runner.os == 'Linux'
        run: |
          # The .deb packages the SAME full PyInstaller tree as the AppImage:
          # bundled GPL ffmpeg, libmpv, and both ASR backends (faster-whisper/
          # ctranslate2 + pywhispercpp/ggml-vulkan). The app is GPL-3.0-or-later,
          # so the bundled GPL components are covered by the source offers in
          # licenses/ffmpeg/README.md and licenses/libmpv/README.md — same as
          # every other artifact. The pre-v2.10 lean deb (stripped ASR/ffmpeg/
          # libmpv) is gone: stripping package dirs could not remove the
          # pure-Python halves from the PYZ inside the executable, so find_spec
          # probes reported ASR as present and the app offered a Download button
          # that could never work. Bundle-parity with the AppImage means the
          # bundled smokes above cover exactly what .deb users run.
          VERSION=${{ steps.version.outputs.version }} ./nfpm package \
            --config packaging/nfpm.yaml \
            --packager deb \
            --target dist/anki-miner_${{ steps.version.outputs.version }}_amd64.deb

      - name: Create archive (macOS)
        if: runner.os == 'macOS'
        run: |
          cd dist
          tar -czf ${{ matrix.artifact_name }}.tar.gz AnkiMiner/

      # Per-OS explicit upload with if-no-files-found: error. A single catch-all
      # step with default 'warn' only fails when NOTHING matches, so a sibling
      # asset filename drift (installer/AppImage/.deb) would upload just the
      # archive and the release would publish incomplete. Listing each OS's full
      # expected asset set and erroring on any miss closes that gap.
      - name: Upload artifact (Linux)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ matrix.artifact_name }}
          if-no-files-found: error
          path: |
            dist/AnkiMiner-${{ steps.version.outputs.version }}-Linux-x86_64.AppImage
            dist/anki-miner_${{ steps.version.outputs.version }}_amd64.deb

      - name: Upload artifact (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ matrix.artifact_name }}
          if-no-files-found: error
          path: dist/${{ matrix.artifact_name }}.tar.gz

      - name: Upload artifact (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ matrix.artifact_name }}
          if-no-files-found: error
          path: dist/AnkiMiner-${{ steps.version.outputs.version }}-Windows-x86_64-Setup.exe

  release:
    needs: build
    # push-only: a dispatch is a dry-run and must NEVER create a GitHub Release.
    # Bare event-name guard with NO always()/!cancelled() wrapper — that preserves
    # GHA's implicit `needs: build` green-gate, so a broken build leg still can't
    # publish a Release.
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: artifacts/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3
        with:
          generate_release_notes: true
          files: |
            artifacts/**/*.tar.gz
            artifacts/**/*-Setup.exe
            artifacts/**/*.AppImage
            artifacts/**/*.deb
